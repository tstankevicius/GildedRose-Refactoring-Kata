{
  "name": "gilded-rose-kata",
  "version": "1.0.0",
  "description": "Gilded Rose kata in TypeScript",
  "scripts": {
    "compile": "tsc --noEmit",
    "start": "ts-node test/golden-master-text-test.ts",
    "test:vitest": "vitest --coverage --mode test",
    "test": "vitest run --coverage --mode test"
  },
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/node": "^18.14.0",
    "@vitest/coverage-istanbul": "^0.28.5",
    "ts-node": "^10.9.1",
    "typescript": "^4.4.4",
    "vite-tsconfig-paths": "^4.0.5",
    "vitest": "^0.28.5"
  }
}
